plugins {
    id 'application'
}

dependencies {
    compile project(':serialization')
    compile project(':node-api')
    compile files("${System.properties['java.home']}/../lib/tools.jar")
}

mainClassName = "net.corda.flow.necromancer.FlowLoader"

